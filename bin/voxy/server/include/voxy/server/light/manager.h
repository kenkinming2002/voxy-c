#ifndef VOXY_SERVER_LIGHT_MANAGER_H
#define VOXY_SERVER_LIGHT_MANAGER_H

#include <voxy/server/export.h>

#include <libmath/vector.h>

/// Light manager.
///
/// Currently, this serves no purpose in the modding API other than the fact it
/// exists internally, and needed to be passed to different functions at from
/// time to time.
struct voxy_light_manager;

#endif // VOXY_SERVER_LIGHT_MANAGER_H
